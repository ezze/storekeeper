language: node_js
node_js:
  - "6"
  - "5"
  - "4"
branches:
  only:
    - master
    - dev
install:
  - npm install
before_script:
  - npm install -g gulp
  - npm install -g webpack
  - npm install -g bower
  - bower install
script:
  - NODE_ENV=production gulp